import React from "react";
import CardProduct from "./CardProduct";

const Products = ({ prodArray }) => {
  //function that paint prodcuts
  //  const paintProduct = {
  //    return prodArray.map(producto =>(
  //     <CardProduct/>

  //    ))

  return <CardProduct />;
};

export default Products;
